import { UserModelResponse } from './user-model-response';

describe('UserModelResponse', () => {
  it('should create an instance', () => {
    expect(new UserModelResponse()).toBeTruthy();
  });
});
